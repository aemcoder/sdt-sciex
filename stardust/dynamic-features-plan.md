<!-- stardust provenance: skill=stardust:dynamics · phase=triage → plan (Phase 3 output; Phases 4/5 not started) · 2026-09-18 · derived from stardust/dynamic-features.md (53 rows) and stardust/dynamics/dynamic-features.generated-plan.{md,json} (48 draft rows, target probe https://main--sdt-sciex--aemcoder.aem.live: 3/3 first-party paths dead) · hands-off mode: interim tiers ship, owner decisions are named assumptions · nothing here is implemented yet. -->

# Dynamic features — plan (sciex.com replica)

Static first, then wire. Every phase ends with the flow verified on the published origin at 1440 and 360, a `parity.json` row, a journal entry and a commit. Only `self` rows run without the owner; the Decision batch in `dynamic-features.md` is one message and each item already has an interim tier.

## Phase order and deliverables

| phase | rows | deliverables | authoring contract | verification (parity.json) | owner decision | effort |
|---|---|---|---|---|---|---|
| **0 · reconcile** | all | re-run `dynamics-plan.mjs --migrated stardust/migrated` once the static recreation has output; mark rows the capture already carries (`delivered-by-capture`); strip `data-di-*`, `data-aue-*`, `data-richtext-*` from imported markup | — | — | none | XS |
| **1 · chrome** | M-01 M-02 M-03 M-11 S-01 S-04 T-11 | `blocks/header` (v3 variant + `template=support-legacy` variant), `blocks/footer` (language switcher), account dropdown logged-out state, search box + content-type `<select>` → `/search-results?term=&contentType=`, print button, native share links | `/nav` and `/footer` documents; nav rows = mega-menu columns; footer "Language" row = `English https://sciex.com/ · Japanese https://sciex.jp/ · Chinese https://sciex.com.cn/ · Korean https://sciex.com/kr`; Login/Create account/SCIEX Now links absolute to `https://sciex.com/…` | `chrome-interaction`: menu opens at 1440/360; `search-redirect`: submitting "analyst" lands on `/search-results?term=analyst&contentType=All`; `link-status` for every absolute sciex.com link (302 to Auth0 accepted) | none (motion-observe evidence) | M |
| **2 · search (interim)** | S-02 S-03 | `/search-results` document + `blocks/search-results` reading `query-index.json` (fields from the Listings contract: `title`, `description`, `content-type`, `path`, `lastModified`), facet by `contentType`, empty state with the captured copy ("We couldn't find anything for …"); `blocks/resourcehub-search` ported from `sciex.com/blocks/resourcehub-search` with Coveo import removed | query index configured at tools.aem.live (`helix-query.yaml` is retired in this repo per AGENTS.md) with `content-type`, `pagetrackingid`, `template`, `locale` columns; every page emits `content-type` metadata | `search-flow`: query "Analyst" returns ≥ 1 result with the KB archetype; `search-empty`: nonsense term shows the empty state; `status` `/search-results` 200 | Coveo reuse (org `danaherproductionrfl96bkr`, searchHub `SCIEXMainSearch`) — when granted, swap the block's data source to Coveo headless with the token scoped to the new origin; second corpora (Training, User guides, Regulatory docs) stay out until then | M |
| **3 · interactive blocks** | M-04 M-05 M-06 M-07 M-08 M-09 L-02 | `blocks/tabs` (Why SCIEX portfolio; accordion < 768), `blocks/card-carousel` (Splide-equivalent, perPage 2/3, arrows + drag), `blocks/testimonial` (fade carousel, quote + attribution), `blocks/page-sub-nav` (anchor scroll-spy, sticky), `blocks/modal` (#getHelpModal logged-out copy), `blocks/sciex-favorite` ported (logged-out state; favourites fetch removed), authored "Newest products" cards | tabs: one row per tab (label · content); carousels: one row per card (image · title · text · link); testimonial: quote · name · role rows; sub-nav: section ids A–D from `#anchor` headings; favourites block rows: id · title · logged-out message · login url · create-account url · view-all text · view-all url (as in the source block) | `interaction`: tab 3 shows its panel; carousel next advances; sub-nav link scrolls; modal opens/closes with focus trap; `no-network` for client-only blocks | none | L |
| **4 · media** | V-01 V-02 V-03 | `blocks/video` (YouTube URL → lite embed with poster; mp4 → `<video controls>`), hero picture from copied DM renditions (`ce-systems-hero-2880x1024.webp`); copy `/content/dam/SCIEX/videos/support/sciexnow-dashboard-video.mp4` to the target | video block row = URL (+ optional poster image); assets under the target's media path | `embed-status`: YouTube ids `_zty54JAgXg`, `TuvjkeyL0ec` load (third-party status recorded, not asserted); mp4 200 | none (ids public; DM asset licence is the owner's own) | S |
| **5 · data** | A-03 A-04 D-01 D-02 CR-01 | `snapshot-api.mjs https://sciex.com/bin/sciex/partners → data/partners/partners.json` + `_provenance.json`; `blocks/contact-information` + `sciex-headquaters` + `icon-card` + `cta-with-text` + `categories` + `breadcrumb` ported from `sciex.com/blocks/*`; `sync-sheets.mjs` for `placeholders.json` (EN column) and `hreflang.json`; `/nav`, `/footer` fragments replace `nav.plain.html`/`footer.plain.html`; breadcrumb from path + index instead of `GET /resource-hub` | sheets `placeholders` (Key · EN) and `hreflang` (path · EN · JP · CN) in the target content source; partners block reads `/data/partners/partners.json` | `data-fed`: contact-us country search "Germany" lists ≥ 1 company; `sheet`: `placeholders.json` 200 with key `my_favorite_resources`; `status` 200 for the three ported pages | partners refresh cadence / later same-origin proxy; EN-only vs all columns | M |
| **6 · forms** | F-01 F-05 | `blocks/embed` for Eloqua iframes (`https://info.sciex.com/LP=4627`, `LP=4625`, inline LP ids on qtof-systems and StatusScope), 100 % × 1500 px; careers page: static intro + CTA to `https://jobs.danaher.com/global/en/sciex` | embed row = iframe URL + title; `/form-pages/product-request` + `/request-information` added to the roster (owner) or linked absolute to the source | `embed-status`: iframe URL responds (third-party status recorded); `link-status` for the careers CTA | roster scope for `/form-pages/*`; Phenom widget vs deep link | S |
| **7 · tags (scaffold)** | T-01…T-09 M-10 A-05 | `scripts/site-config.js` with every vendor id **disabled**: OneTrust `0dec26b6-99d0-4899-bb84-862e1aef78ce`, GTM `GTM-WMZL3B` (+ `GTM-5J97L4S`, `GTM-KX7X4W`, `GTM-PDRV95V`, `GTM-WW7MKD` recorded), GA4 `G-KD23C87L58`, Dreamdata, Fathom, Qualtrics `ZN_1o1ioypsMWWxBmB` / `ZN_b4z8pJnZ6X9z32B`, WalkMe `1e111ec60eee4eb8859b4147fa4ea483`, Salesforce MC Personalization `sciex_prod`, Decibel; consent-gated `delayed.js` loader; `dataLayer` contract (`pagetrackingid`, `user` slot left empty) | metadata `pagetrackingid` per page (from the source `<meta name="pagetrackingid">`) | `tag-absent` while disabled (no request to the vendor hosts); after enable: `tag-fires` per vendor with consent accepted | which tags run on the new host and under which ids (one batch) | S |
| **8 · locale** | I18N-01 | footer switcher links (Phase 1) are the interim; if `/kr` is in scope: locale tree under `/kr/` with `hreflang` from the D-02 sheet | `/kr/**` documents mirror EN paths; `hreflang` rows drive `<link rel=alternate>` | `hreflang`: EN archetypes emit en-US + x-default (+ ko-KR when `/kr` ships); `status` `/kr/` 200 when in scope | scope of `/kr`; JP/CN remain external hosts | M (if in scope) |
| **9 · register / verify** | A-01 A-02 F-02 F-03 F-04 T-10 T-13 X-01…X-04 T-12 L-01 | register rows with production statements (already in `dynamic-features.md`), `stardust/dynamics/parity.json` covering every phase, `dynamics-check.mjs --origin https://main--sdt-sciex--aemcoder.aem.live` → `stardust/qa/dynamics-report.md`; T-12 GeoIP consumer check in a human browser (two regions); `/events` listing when the roster decision lands | — | replay of the closed check set; third-party statuses recorded next to every assertion | backend statement confirmation (informational); GeoIP consumer; `/events` roster | S |

Effort key: XS < ½ h · S ½–2 h · M 2–6 h · L 6–12 h.

## Interim tiers that ship without the owner (named assumptions)

- Search: query-index results page instead of Coveo (**assumption: Coveo not yet allowed on the new origin**).
- Tags: all scaffolded, none firing (**assumption: no vendor id is enabled until the owner confirms**).
- Locale: footer links to source hosts; no `/kr` tree (**assumption: Korean is a later wave**).
- Forms: Eloqua iframes embedded as-is; careers deep-links (**assumption: widget embed deferred**).
- Auth/commerce/community: logged-out chrome, absolute links to `https://sciex.com/bin/sciex/login`, `shop.sciex.com`, `training.sciex.com`, `community.sciex.com` (**production statement, see Register**).
- Data: one-time snapshot of `/bin/sciex/partners`; EN placeholders only.

## Hard blockers seen

None. Source reachable; target config writable is not yet needed (no endpoint indirection in the interim tiers). No regulated-pii form exists on the crawled pages (F-01 is marketing lead capture on Eloqua, kept as an iframe).

## Tooling notes

- `dynamics-detect.mjs` misses `/bin/sciex/currentuserdetails` (text/html 403, path not in `API_PATH`) and `/bin/sciex/partners`, `/resource-hub`, `nav.plain.html` fragments (only seen on pages it did not probe); they were added from the crawl's `dynamic` sections. Suggest widening `API_PATH` to `/bin/` (AEM servlets) and accepting `text/html` XHR under first-party as "A" candidates.
- `d-first-party-data-file-get-libs-granite-csrf-token-json` is classed D by the detector; it is an AEM CSRF endpoint (A-02).
- The vendor table classified `c360a.salesforce.com` as "form backend: Salesforce / Pardot"; it is Salesforce Data Cloud / MC Personalization (T-05). Rows for WalkMe, Dreamdata, MaxMind and `geolocation.onetrust.com` were added to `stardust/scripts/dynamics/vendors.json` (copy only; plugin table untouched).
- `--from-state` yields 2 URLs here because `state.json` pages carry `type: null`; `--urls` was used with the 5 archetypes.
- `playwright-cli` is not on PATH; not needed for Phases 1–3 (the detector uses the `playwright` module directly). Phase 4 motion-observe evidence will need it or an equivalent script.
- No `stardust/migrated` yet — reconciliation (`--migrated`) deferred to Phase 0 / rollout B2.
