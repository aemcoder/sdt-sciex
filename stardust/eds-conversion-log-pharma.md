# EDS conversion log — `/applications/pharma-and-biopharma` (v3 application landing)

Owner: deploy phase-1 agent (convert + local gates). Coordinator merges into `stardust/eds-conversion-log.md`.
Prototype (visual spec): `stardust/prototypes/applications-pharma-and-biopharma-proposed.html` (served `http://localhost:8805/…`).
Schema: `stardust/eds-schema/applications-pharma-and-biopharma.json` (10 sections). Content: `content/applications/pharma-and-biopharma.html`.

## Step 2 triage — is it a block (D1)? collection match (D11)? name · tier · shape

| # | prototype section (`data-section`) | D1 | D11 | EDS authoring | decode tier | shape |
|---|---|---|---|---|---|---|
| 1 | `breadcrumb` (nav#breadcrumb) | block (bespoke nav structure, shared by every interior page) | none → invented name `breadcrumb` | **`breadcrumb`** — 1 row · 1 cell · `<ul>` of links, LAST `<li>` plain text (current page). First `<li>` = `<a href="/">Home</a>` (text hidden, house icon via CSS mask). Shared contract with the KB page. | reconstructive-light: the `<ul>` MOVES into `nav > .container`; last li text is wrapped in `<a aria-current="page" href=location.pathname>` (live renders the current crumb as a link) | simple |
| 2 | `hero-text` (h1 + lede) | **default content** (D1) — `<h1>` + `<p>` in the SAME section as the breadcrumb block | — | `.breadcrumb-container .default-content-wrapper` rules in `blocks/breadcrumb/breadcrumb.css` (interior-page h1 = live `.text-alfa` 34/42 → 64px @1280; lede grey-500, 75 % / 50 %) | — | — |
| 3 | `page-image` (full-bleed 1440×540 picture) | **default content** — `<p><img></p>` after the lede, same section | — | full-bleed breakout (`width:100vw; margin-left:calc(50% - 50vw)`) via `.breadcrumb-container .default-content-wrapper p:has(> picture, > img)` in breadcrumb.css; carries pb 32/48 (the live `.sec`) | — | — |
| 4 | `sub-nav` (sticky scroll-spy) | block (interactive) | none → `sub-nav` | **`sub-nav`** — 1 row · 1 cell · `<ul>` of `<a href="#<heading-id>">Label</a>` (simple inferred list, D5). Hrefs = the pipeline's heading ids (`#overview`, `#pharma-and-biopharma-applications`, `#perspectives`, `#thought-leaders`); labels verbatim (Overview · Applications · Perspectives · Stories) | reconstructive-light: `<ul>` MOVES into the bar; target = `getElementById(hash)` → its `main > .section` (fallback: heading/eyebrow whose slug = hash — harness has no pipeline ids; the block also sets the section id when nothing carries it so deep links work). Sticky = the block's own SECTION (`main .section.sub-nav-container.sticky-subnav`, class added on first scroll like live); spy threshold = nav height; click glide = section top − 52 px | simple |
| 5 | `overview` (h2 + 2 p) | **default content** (D1) in its own section | — | section-metadata `style: lead` — **needs foundation CSS (styles.css frozen for this agent) → see § Foundation additions** | — | — |
| 6 | `applications` (h2 + lede + 7 image cards, 1/2/4-up) | block (repeating units) | `cards` | **`cards image`** — head (h2 + p) is DEFAULT CONTENT before the block (reabsorbed, EW8, head paints inside the bordered head row); 1 row per card: picture \| body (`<h3>`, `<p>`, `<p><em><a>Learn more</a></em></p>`) | reconstructive (existing `classify` + `buildCard`); new `decorateImage` appended | container |
| 7 | `perspectives` (dark fade quote carousel, 3 slides) | block (interactive, repeating) | `carousel` (Block Collection: 1 row per slide, image \| content) | **`carousel quotes`** — eyebrow `<p>Perspectives</p>` is DEFAULT CONTENT before the block (prototype paints it INSIDE the dark band → reabsorbed EW8); 1 row per quote: portrait picture \| body (`<p>“quote”</p><p><strong>Name</strong></p><p>Role</p>` — name = leading preserved tag) | reconstructive; slides MOVE authored nodes; controls + "N / total" pagination generated (allowlisted runtime numerals) | container |
| 8 | `thought-leaders` (3 story cards, rail < 1024) | block | `cards` | **`cards stories three-up`** (REUSE `stories`; `three-up` modifier = live Splide perPage 3 + t-charlie title) — head `<h2>` default content (existing reabsorb); 1 row per card: picture \| body (`<h3>`, `<p>`, `<p><em><a target=_blank>Watch now</a></em></p>`) | existing `decorateStories` (rail mechanics identical) | container |
| 9 | `connect` (h3 "Let's connect" + p) | **default content** — canonicalised to `<h2>` (section title, no level jump) + `<p>`, in the SAME section as the banners block | — | styled by `blocks/columns/columns.css` via `.columns-container .default-content-wrapper:has(+ .columns-wrapper .columns.banners)` (live `.text-echo` 20/26 → 24px) — variant-scoped without JS | — | — |
| 10 | `linkedin-banners` (2 linked 600×140 images) | block (2 columns) | `columns` | **`columns banners`** — 1 row · 2 cells · each `<p><a target=_blank><img></a></p>` | stock `columns.js` (cells become `.columns-img-col`); variant CSS appended | container |
| — | SCIEX Now band (chrome on live) | block | — | **`support-band`** reused verbatim from `content/index.html` | existing | — |

Metadata block: Title `Biopharma / pharma research` (live `<title>`), Description verbatim from `stardust/current/pages/applications-pharma-and-biopharma.json`.

## Deliberate decisions / residuals
- Section head reabsorption: `cards image` and `carousel quotes` MOVE the preceding `.default-content-wrapper` children (EW8). `cards stories` already does.
- Breadcrumb section round-trip unit = breadcrumb + h1 + lede + picture (one section) → the roundtrip reports the h1/lede as EXTRA (🟠/🟡 advisory) against `[data-section=breadcrumb]`; the prototype's `hero-text`/`page-image` sections have no block to pair with. Accepted.
- Current-page crumb: authored as plain text (shared contract); decode links it to `location.pathname` with `aria-current="page"` — no words added.
- Banner alt text: live `alt=""` on link-only images; authored `alt` names the newsletter (a11y: the image is the link's only content). Card/portrait images keep `alt=""` (decorative, title/name follows).
- Card titles `<div class="card__title">` → `<h3>`; connect `<h3>` → `<h2>` (real outline).
- LinkedIn spacer `<p>&nbsp;</p>` paragraphs (#112) modelled as `columns.banners` padding, never authored.
- Images: authored `https://content.da.live/aemcoder/sdt-sciex/media/applications/<file>` (uploads listed in ready.json); the two thought-leader images already live under `/media/home/` from the home page and are reused.
- Motion implemented (observed on live): sticky sub-nav + `.no-scrollbar`, scroll-spy `.active` underline .5s, click glide to section − 52 px (`prefers-reduced-motion` → instant), testimonial fade .4s cubic-bezier(.25,1,.5,1) with is-prev/is-active/is-next + `N / total` pagination + disabled end arrows, stories rail (existing block), hover states (card image scale, link-arrow, sub-nav link colour, enabled arrow fill). Dead on live (breadcrumb hover, pagination hover) not implemented.

## Foundation additions required (styles/styles.css — coordinator applies; this agent may not write styles/*)
Section style value **`lead`** for interior default-content prose (live `.atomic-richtext text-lg` on grey-500; Overview #A). Ready-to-append file: `stardust/.work/deploy/applications-pharma-and-biopharma/styles-addendum.css`. Until applied, the Overview section renders in the foundation's 15/23 black body type (structure/gates unaffected).

## Gate notes (phase 1, local)
- **davids-model-lint**: 0 🔴, 3 🟡 D1 "single-column 1-row block holding prose" — `breadcrumb` (bespoke nav structure, icon home + chevrons + current-page semantics), `sub-nav` (interactive sticky scroll-spy — client-only behaviour, D1 exception), `support-band` (existing site band, reused verbatim). Justified.
- **block-roundtrip**: against the pristine prototype the only 🔴s are 10 ROLE SWAPs "body in the proto, heading in EDS" for the card titles — the pharma prototype tags them `div.card__title` (as live does) while the ENCODE contract and the site's existing `cards` authoring (home) make card titles `<h3>` ("canonicalising a prototype's card title to `<h3>` is correct"). Re-run against a byte-identical fixture whose ONLY change is `div.card__title` → `h3.card__title` (`stardust/.work/deploy/applications-pharma-and-biopharma/proto-canon/`, canon.css resets headings to `font: inherit` so pixels are unchanged): **exit 0, all blocks closed, EW 52/52 editable, 0 dead, 0 duplicated**. Advisories: breadcrumb unit shows the h1/lede/picture as EXTRA (the prototype's hero-text/page-image sections have no block to pair with — same section by design); columns unit shows the connect head as EXTRA (same reason).
- **qa-gate**: the stock gate pairs schema sections with page blocks BY ORDER (its own comment calls this fragile); four prototype sections are D1 default content here (hero-text, page-image, overview, connect) so the pairing slips by four. Run with a mechanically derived blocks-only schema (`stardust/.work/deploy/applications-pharma-and-biopharma/schema-blocks-only.json` = canonical schema minus those four sections): **PASS 31 ok / 5 warn / 0 fail**. The wide-1600 warnings (1536 px content) match the prototype's `.container` (max 1536 at ≥1536); columns.banners = 1408 = container inner width at 1600.
- **ew-editability-probe --simulate-editor**: 55/55 editable, 0 dead, 0 dup, 0 drift, block Δh 0 (a first run showed the hero lede drifting 48→72 px in edit mode: `align-items: flex-start` on the flex wrapper let the editor's div shrink-to-fit — removed).
- **Interaction drive** (Playwright, `drive.mjs`): sticky-on-scroll + no-scrollbar, scroll-spy `.active` + underline, glide landing = section top − 52 px, testimonial fade (mid-transition opacity .195/.805 — identical to the live measurement), `N / total` pagination, disabled end arrows, pagination click, stories rail @360 (controls mount, translateX −324 px = live), 3-up @1440 without controls, card hover scale(1.05), 0 pageerrors.
- **Known gap in the SHARED `cards stories` variant (not touched — concurrent edit)**: its pagination numerals are static (`1 / `, `2 / `, `3`) and never re-render the current index; live Splide reads `N / total` (first button = current index, middle buttons empty, last = total) — with 3 slides at <1024 this is visible. Coordinator: align `decorateStories` paint with the `carousel quotes` `paginate()`.
- Harness note: the vendored `aem.js` has no client-side section-metadata handling (rendering v2 applies it server-side), so the local harness copy emulates the pipeline (`style: lead` → section class, config block removed) and rewrites `content.da.live` media to the local prototype assets for the eyeball/drive copy only (`pharma-local.html`). Authored content is untouched.
